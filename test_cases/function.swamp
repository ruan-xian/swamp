0=| Expected value: 0 |=0
0=| did not implement function things in calc.ml so expected value is not correct |=0
let f1 = fun(x, y) -> let c = 2 in c * x + y in
let f2 = fun() -> 2 * (3 + 4) in
let f3(x) = x - 3 + 1 in

let k = f1(2, 3) in
k + (fun(x) -> x * x)(2)
